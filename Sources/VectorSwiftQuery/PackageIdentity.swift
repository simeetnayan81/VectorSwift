import VectorSwiftCore
import VectorSwiftIndex

/// Identity marker for the Query module (search planning and multi-segment merge).
public enum VectorSwiftQueryModule: Sendable {
    public static let moduleName = "VectorSwiftQuery"
}
