## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `./scripts/test.sh` passed **before this commit** (full unit suite; commit gate)
- [ ] `./scripts/build.sh --release` passed locally (library only)
- [ ] GitHub **CI** check is green (build macOS + Linux, test Linux CPU + macOS/MLX, example both)
- [ ] Tests cover new behavior / edge cases
- [ ] No secrets committed
- [ ] README updated if user-facing behavior changed
- [ ] No story/phase ids (`S16`, `Phase 1`, …) in Sources / Tests / Examples / README

## Test plan

<!-- e.g. ./scripts/test.sh — N tests, 0 failures; ./scripts/build.sh --release -->
